STAGE 
    hijau + u = untracked
    hijau + a = added(baru ditambahkan)
    kuning + m = modified(dirubah)
    putih = selesai


AWAL
     git init nama_project/folder
     git init .


LANJUTANNYA
    menambahkan file ke unstagged
        git add nama_file
        git add.

    commit file  
        git commit -m "keterangan commit"

    check commit
        git log
        git log --oneline

    cancel perubahan
        git checkout nama_file